Pi-agents keeps delegated workflows observable and recoverable with Pi 0.84. Live agent output continues during streaming responses, partial text survives early process exits, and transient model-catalog refresh failures can retry on later events.

## 🐞 Bug fixes

### Pi 0.84 delegated agent compatibility

Delegated workflows remain observable and recover cleanly with Pi 0.84. Live agent output continues updating while responses stream, partial text is preserved when a delegated process exits early, and transient model-catalog refresh failures can retry on later events.

*By @mavam and @codex in #51.*
